import {createSelector} from 'reselect'


export const selectFormState = state=>state.formState;

// export const select=createSelector(
//     [selectFormState],
//     formState=>stickers.stickersBundle
// );
